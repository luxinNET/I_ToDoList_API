spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/itodo}
    username: ${DB_USERNAME:itodo_app}
    password: ${DB_PASSWORD:itodo_dev_password}

logging:
  level:
    com.example.itodo: DEBUG
    org.springframework.security: INFO
